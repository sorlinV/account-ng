export class Transfer {
        public date:string;
        public desc:string;
        public sum:number;
        public cath:string;
        constructor (date, desc, sum, cath) {
            this.date = date;
            this;desc = desc;
            this.sum = sum;
            this.cath = cath;
        }
}